# -*- coding: utf-8 -*-
"""母差保护 主Agent Prompt"""


def build_main_agent_prompt(paragraphs_content: str) -> str:
    return f"""# 主 Agent 母差跳闸简报生成 Prompt

## 任务

**只读段落文件**，基于所有 Subagent 生成的装置段落，生成完整的母差跳闸简报。

**严格禁止：读取原始 hdr/rms/events/csv/dat 文件**

## 输入段落

{paragraphs_content}

## 母差保护简报结构

请按以下结构生成简报：

**严禁在简报中输出模板说明、填写规则、注释等内容。只输出简报正文，不要输出任何指导性文字。**

```markdown
# {{日期}} {{厂站名称}}母差跳闸简报

## 一、故障基本情况

根据录波分析母差故障基本情况：故障时间、厂站、故障设备、故障相别、故障类型。

| 项目 | 内容 |
|------|------|
| 故障时间 | {{从段落提取}} |
| 厂站 | {{从段落提取}} |
| 故障设备 | {{母线名称}} |
| 故障段 | {{I母/II母}} |
| 故障相别 | {{综合判断}} |
| 故障类型 | 瞬时性/永久故障 |
| 保护动作情况 | {{各套动作汇总}} |
| 故障电流 | {{取自段落 HDR FaultInfo 故障相电流（一次值），不使用 RMS 最大值或突变值}} |

## 二、保护配置情况

列出母差保护装置配置信息。

### 2.1 保护装置信息表
| 厂站 | 套别 | 装置型号 | 制造厂家 | 软件版本 | 备注 |
|------|------|----------|----------|----------|------|

### 3.2 母差保护各支路CT变比及运行母段

> **母差保护必有此节**。从段落"支路配置"表提取所有投运间隔的 CT 变比与运行母段。母联/分段须说明其连接哪两段母线。故障段间隔须**加粗或⚡标识**。

| 支路名称 | 间隔类型 | CT变比（一次/二次） | 连接/运行母段 | 故障前运行状态 | 两套一致性 | 备注 |
|----------|----------|---------------------|---------------|----------------|------------|------|

**填写规则**：
- CT变比来源：各支路 CT 变比须从段落"支路配置"表提取，不得仅引用基准 CT
- 母联/分段：标注其连接的两段母线名称（如"互联ⅠA母↔Ⅱ母"）
- 故障段间隔：加粗或⚡标识
- 两套一致性：逐间隔比对 CT 变比、母段归属，一致填"一致"，不一致须标注差异

### 3.3 接线与母线运行方式

> 从段落提取接线形式、母联/分段位置、互联/分列软压板状态。

```
[接线形式]：[如 220kV 双母单分段（ⅠA母 / ⅠB母 / Ⅱ母）]
故障前运行方式：[正常并列运行 / 互联运行 / 分列运行]
- 母联/分段位置及状态
- 互联/分列软压板状态
```

## 三、动作基本情况

描述各套母差保护的动作过程，所有时间使用绝对时间HH:MM:SS.mmm。
{{按套别叙述}}

## 四、动作时序表

### 4.1 时间对齐基准

**第一步：电流正突变对齐**

| 套别 | 正突变最大值(A) | 正突变绝对时间 |
|------|----------------|----------------|

**第二步：保护启动对齐**

### 4.2 动作时序表（仅动作事件）

*基准时间：{{最早的保护启动绝对时间}}*

| 绝对时间 | 第1套 | 第2套 |
|----------|-------|-------|
| HH:MM:SS.mmm | 事件 | - |

## 五、通道对应关系准确性分析

### 5.1 间隔通道映射验证

| 间隔 | A相通道 | B相通道 | C相通道 | 映射结果 |
|------|---------|---------|---------|----------|

**检查内容**：各间隔电流通道是否正确对应到该间隔，通道接反/错位检查。

### 5.2 两套母差各支路电流一致性校验

| 支路名称 | 母段 | 第1套(A) | 第2套(A) | 偏差(%) | 结果 |
|----------|------|----------|----------|---------|------|

> 从段落 RMS 各间隔电流提取，两套对比。电流>0.1A时偏差<5%为一致，<0.1A为微小电流比较绝对差值。

## 六、差流分析

### 6.1 大差与各段小差（按母段分别列出）

| 差流项目 | 母段 | 第1套(A) | 第2套(A) | 一致性 | 性质/说明 |
|----------|------|----------|----------|--------|-----------|
| 大差电流 | 全站（除母联/分段） | | | | 非穿越性 |
| ⅠA母小差 | ⅠA段 | | | | 非故障段→应≈0 |
| ⅠB母小差 | ⅠB段 | | | | 非故障段→应≈0 |
| Ⅱ母小差 | Ⅱ段(故障段) | | | | 故障段≈故障电流 |

**差流性质判定**：故障段小差非穿越性 + 非故障段小差≈0 + 各间隔电流矢量和验证 → 区内故障、选择性正确。

### 6.2 各间隔电流详情（矢量和验证）

**故障段（II母）各间隔A相电流**：
| 间隔 | A相电流 | B相电流 | C相电流 | 相位 |
|------|---------|---------|---------|------|
| （从段落 RMS 各间隔电流提取） |
| **矢量和** | | | | |

> 故障段各间隔电流矢量和应≈大差电流，验证差流计算正确。

## 七、保护动作评价

评价母差保护动作是否正确。

## 八、故障发展过程分析（仅复杂/发展性故障，单一阶段故障不设本节）

| 阶段 | 故障段母线UA/V | UB/V | UC/V | 各间隔电流(摘要) | 判读 |
|------|------|------|------|------|------|
| 起始 | | | | | |
| 发展 | | | | | |
| 全盛 | | | | | |
| 切除 | | | | | |

**关键事件**（绝对时刻）：故障电流启动 / 各相电压塌缩 / 发展转折 / 切除（电流归零）
**发展性判读**：□ 是发展性故障 □ 非发展性
**电弧性/稳定性**：□ 电弧性不稳定短路 □ 稳定短路

> 从段落"故障发展过程数据"区块提取。无数据时省略本节。

## 九、故障录波评价

| 评价项目 | 评价结果 | 说明 |
|----------|----------|------|
| 故障录波数据 | 有/无 | |
| 动作时间一致性 | 一致/不一致 | |
| 故障电流一致性 | 一致/偏差较大 | |
| 通道对应一致性 | 一致/异常 | 通道映射与保护录波是否一致 |
```

## 可信度原则

**只写有依据的数据，不确定的数据必须标注。**

### 交叉验证
| 校验项 | 规则 | 异常标注 |
|--------|------|----------|
| 大差/小差 | 大差 = 各间隔电流矢量和 | `[待核实: 差流计算异常]` |
| 动作时间 | 同厂站两套动作时间差 < 20ms | `[待核实: 动作时间差较大]` |
| 通道对应 | 同一间隔两套保护电流应一致 | `[待核实: 通道映射不一致]` |

### 不确定数据的处理
- 文件缺失 → 标注 `[本套文件缺失]`
- 字段无数据 → 标注"无数据"
- 数据矛盾 → 标注矛盾，不强行统一

## 时序信息融合规则

**核心原则：动作时序表必须使用绝对时间（HH:MM:SS.mmm），所有事件必须转换为统一的绝对时间后填入时序表。严禁使用相对时间（ms偏移量）作为时序表的时间列。**

### 绝对时间计算方法

| 时间来源 | 转换规则 |
|----------|----------|
| Events CSV | 已是绝对时间（HH:MM:SS.mmm），直接使用 |
| HDR TripInfo 偏移量 | **绝对时间 = 该套电流正突变绝对时间 + HDR偏移量**（首选）；若无正突变数据，用 Events 中保护启动绝对时间作为基准 |
| 段落中的时间 | 已是绝对时间格式（HH:MM:SS.mmm），直接使用 |

### 常见错误（严禁）
- ❌ 将某套 HDR 的偏移量（如 `20ms`）直接当作全局相对时间填入时序表
- ❌ 用某个统一基准时间减去各套事件时间来生成"全局相对时间"列

### 时序表填写规则
1. 时间列必须为绝对时间格式 `HH:MM:SS.mmm`
2. 每个事件的绝对时间必须独立计算，不同装置有不同的时间基准
3. 动作时序表中不包含电流正负突变行
4. **故障电流必须使用段落 HDR FaultInfo 中的故障相电流（一次值），不得使用 RMS 最大值或电流突变值**
4. HDR 中有但 Events 中无的信号，在时序表中标注 `_hdr` 后缀
"""
