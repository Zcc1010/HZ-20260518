# -*- coding: utf-8 -*-
"""线路保护 主Agent Prompt"""


def build_main_agent_prompt(paragraphs_content: str) -> str:
    return f"""# 主 Agent 跳闸简报生成 Prompt

## 任务

**只读段落文件**，基于所有 Subagent 生成的装置段落（Markdown 格式），生成完整的线路跳闸简报。

**严格禁止：读取原始 hdr/rms/events/csv/dat 文件**

## 核心原则（必须遵守）

1. **严禁编造数据**：所有数据（设备编号、变比、定值、电流、电压等）必须来自输入段落，不得凭空推测或编造。无数据时标注 `[无数据]`，不得留空或编造。
2. **数据溯源**：每一项数据必须明确来源（来自哪个段落、哪个文件），不得在无依据的情况下补充数据。
3. **保留矛盾**：数据矛盾时标注异常，不强行统一数据。
4. **结论固定**：同一份数据的分析结论必须一致，不得出现前后矛盾的判定。

## 输入段落

{paragraphs_content}

## 线路保护简报结构

请按以下结构生成简报（注意：模板中的 `{{xxx}}` 表示需要填入实际值的位置，无数据时表格内填 `-`，非表格位置写明缺失原因，**严禁输出 `{{xxx}}` 占位符**）：

```markdown
# {{日期}} {{线路名称}}跳闸简报

## 一、故障基本情况

| 项目 | 内容 |
|------|------|
| 故障时间 | {{从段落提取}} |
| 厂站 | {{从段落提取}} |
| 故障设备 | {{线路名称}} |
| 故障相别 | {{综合判断}} |
| 故障类型 | 瞬时性故障/永久故障 |
| 保护动作情况 | {{各套动作汇总}} |
| 故障电流 | {{取自段落 HDR FaultInfo 故障相电流（一次值），不使用 RMS 最大值或突变值}} |
| 故障测距 | {{各套测距}}，线路全长{{值}}km |
| 重合闸情况 | {{重合闸方式}} |

## 二、保护配置情况

### 2.1 保护装置信息表

| 厂站 | 套别 | 装置型号 | 制造厂家 | 软件版本 | 备注 |
|------|------|----------|----------|----------|------|

### 2.2 通道配置

{{CT变比、PT变比、线路全长、差动定值等}}

## 三、动作基本情况

{{从各套段落的Events信息和HDR信息-TripInfo中提取动作信息，按厂站/套别排列}}

## 四、动作时序表

### 4.1 时间对齐基准

**第一步：电流正突变对齐**

| 厂站 | 套别 | 正突变最大值(A) | 正突变绝对时间 |
|------|------|----------------|----------------|

**第二步：保护启动对齐**

### 4.2 动作时序表（仅动作事件）

*基准时间：{{最早的保护启动绝对时间}}*

| 绝对时间 | 厂站1第1套 | 厂站2第1套 | 厂站1第2套 | 厂站2第2套 |
|----------|-----------|-----------|-----------|-----------|
| HH:MM:SS.mmm | 事件 | - | - | - |

## 五、综合分析

### 5.1 两套保护动作一致性
- 动作逻辑是否一致
- **启动时间差异**: 同厂站两套启动时间差 > 20ms时，需单独分析原因（采样同步、装置响应速度等），并明确提示差异值

### 5.2 两侧差流分析

从各段落中直接提取差流数据（含变比信息）：

| 厂站 | 套别 | CT变比 | 通道一A相差流 | 通道一B相差流 | 通道一C相差流 | 通道二A相差流 | 通道二B相差流 | 通道二C相差流 | 故障相电流 | 最大差动电流 |
|------|------|--------|---------------|---------------|---------------|---------------|---------------|---------------|------------|--------------|
| {{厂站}} | {{套别}} | {{CT一次}}/{{CT二次}} | {{值}} A | {{值}} A | {{值}} A | {{值}} A | {{值}} A | {{值}} A | {{值}} A | {{值}} A |

（数据来自各段落的 HDR FaultInfo 和定值信息，获取不到时写"未获取到数据"）

- 分析产生的原因。

### 5.3 故障测距验证

**测距方向规则**: 0km表示本侧出口，线路全长表示对侧出口
**验证公式**: 本侧测距 + 对侧测距 ≈ 线路全长（±20%）

按以下格式输出（每项之间必须用空行分隔，确保 Markdown 渲染为独立段落）：

**本侧测距**: {{厂站名_套别}} 测距 {{值}} km

**对侧测距**: {{厂站名_套别}} 测距 {{值}} km（如有对侧数据）

**线路全长**: {{值}} km（取自段落中的线路总长度定值）

**验证计算**: {{本侧测距}} + {{对侧测距}} = {{和}} km

**与线路全长偏差**: {{偏差值}} km（{{百分比}}%）

**结论**: 测距结果{{正常/异常}}，{{简要说明}}

测距结果异常时，需明确指出哪一侧测距异常，并分析可能原因。

### 5.4 重合闸分析

### 5.5 保护动作持续时间分析
| 厂站 | 套别 | 保护类型 | 启动时间 | 返回时间 | 持续时间 | 备注 |
|------|------|----------|----------|----------|----------|------|
- 各套保护动作持续时间（启动→返回）是否一致
- 距离I段、差动保护等动作时间差异是否在合理范围内
- 异常时标注 `[待核实: 动作时间差异较大]`

## 六、故障判定结论

## 七、故障录波评价（如有）
```

## 可信度原则

**贯穿始终的核心原则：只写有依据的数据，不确定的数据必须标注。**

### 禁止使用占位符（最高优先级）

**严禁输出任何含花括号的占位符。** 这是最高优先级规则，违反即为失败。

禁止的模式（包括但不限于）：`{{值}}`、`{{结果}}`、`{{厂家名称}}`、`{{程序版本}}`、`{{CT一次}}`、`{{CT二次}}`、`{{百分比}}`、`{{正常/异常}}`、`{{和}}`、`{{偏差值}}`、`{{简要说明}}`、`{{厂站名_套别}}`、`{{本侧测距}}`、`{{对侧测距}}`、`{{套别}}`、`{{厂站}}`、`{{间隔1}}`、`{{间隔名}}`、`{{通道名}}`、`{{侧别}}`

**处理规则：**
- 模板中的 `{{xxx}}` 是提示你该填什么内容的说明，**不是让你输出的文本**
- 如果段落中有数据 → 填入实际数值
- 如果段落中无数据 → 表格内填 `-`，非表格位置写"未获取到数据"
- **绝对不允许**直接输出含花括号的文本

### 数据溯源
- 每一项数据必须明确来源（来自哪个段落、哪个文件）
- **不得**在无依据的情况下补充数据

### 交叉验证
| 校验项 | 规则 | 异常标注 |
|--------|------|----------|
| 测距方向 | 0km为本侧，线路全长为对侧；本侧测距 + 对侧测距 ≈ 线路全长（±20%） | `[待核实: 测距偏差过大]` |
| 差流 | 两侧差流一次值偏差 < 10%；差流必须从FaultInfo的Idiff字段提取 | `[待核实: 差流偏差较大]` |
| 动作时间 | 同厂站两套启动时间差 < 20ms为正常；20-50ms需提示；>50ms为异常 | 正常：无标注<br>较大：`[待核实: 启动时间差异较大，差XXms]`<br>异常：`[异常: 启动时间差异过大，差XXms，需检查同步采样]` |
| 电流一致性 | 保护录波与故障录波同相电流偏差 < 20% | `[待核实: 电流一致性存疑]` |
| 变比一致性 | 同厂站两套保护CT变比应一致 | `[待核实: 同厂站CT变比配置不一致]` |

### 不确定数据的处理
- **文件缺失** → 标注 `[本套文件缺失]` 或 `[录波文件缺失]`
- **字段无数据** → 表格内用 `-` 表示，非表格位置写"未获取到数据"
- **数据矛盾** → 标注 `[相别矛盾]` / `[测距异常]` 等，不强行统一

## 时序信息融合规则

**核心原则：动作时序表必须使用绝对时间（HH:MM:SS.mmm），所有事件必须转换为统一的绝对时间后填入时序表。严禁使用相对时间（ms偏移量）作为时序表的时间列。**

### 绝对时间计算方法

| 时间来源 | 转换规则 |
|----------|----------|
| Events CSV | 已是绝对时间（HH:MM:SS.mmm），直接使用 |
| HDR TripInfo 偏移量 | **绝对时间 = 该套电流正突变绝对时间 + HDR偏移量**（首选）；若无正突变数据，用 Events 中保护启动绝对时间作为基准 |
| 段落中的时间 | 已是绝对时间格式（HH:MM:SS.mmm），直接使用 |

### 常见错误（严禁）
- ❌ 将某套 HDR 的偏移量（如 `20ms`）直接当作全局相对时间填入时序表
- ❌ 用某个统一基准时间减去各套事件时间来生成"全局相对时间"列

### 时序表填写规则
1. 时间列必须为绝对时间格式 `HH:MM:SS.mmm`
2. 每个事件的绝对时间必须独立计算，不同装置有不同的时间基准
3. 列顺序：厂站1第1套、厂站2第1套、厂站1第2套、厂站2第2套
4. 动作时序表中不包含电流正负突变行
6. **故障电流必须使用段落 HDR FaultInfo 中的故障相电流（一次值），不得使用 RMS 最大值或电流突变值**
5. HDR 中有但 Events 中无的信号，在时序表中标注 `_hdr` 后缀
"""
