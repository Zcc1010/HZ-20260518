# -*- coding: utf-8 -*-
"""线路保护 Subagent Prompt"""


def build_subagent_prompt(
    hdr_content: str,
    rms_content: str,
    events_content: str,
    station: str,
    set_number: str,
) -> str:
    return f"""# 线路保护 Subagent Prompt 模板

## 任务

给定本套保护装置的录波文件（hdr + rms.csv + events.csv），按 HDR/Events/RMS 三个区块提取本套装置的关键信息，生成标准化的 Markdown 段落。

## 核心原则（必须遵守）

1. **严禁编造数据**：所有数据必须来自输入文件，不得凭空推测或编造设备编号、变比、定值等信息。无数据时标注 `[无数据]`。
2. **数据溯源**：关键数据必须标注来源文件和字段名。
3. **保留原始值**：提取的数据必须与源文件一致，不得修改或"修正"原始数值。

## 设备信息

- 厂站: {station}
- 套别: {set_number}

## 电压等级与接地方式识别（前置条件，必做）

1. **电压等级**：从 PT 变比（500kV/100V、220kV/100V、110kV/100V、35kV/100V、10kV/100V）、线路额定电压或 HDR 系统参数判定属于 **500（含 750/1000kV）/ 220 / 110 / 66 / 35 / 10(6) kV**。
2. **套别（配置数）**：
   - 500kV+ / 220kV+ → 双套（每端 2 套，共 4 套）。
   - 110kV 及以下 → 单套（双端每端 1 套 = 双端单套；单端 = 单端单套）。
   - **单端单套"装置内双套电流"红线**：若本装置记录两套电流（保护电流1/2），判为装置内本地量，**绝非本侧/对侧**，禁止当作纵差差流。
3. **接地方式**：从 HDR 定值（"小电流接地""消弧线圈""经电阻""直接接地"）或故障现象判定（大电流=故障相电流大且不对称；小电流=故障相电压↓电流小、三相基本对称仅零序电压出现）。
4. **接线方式与开关保护（500kV+ 关键）**：若接线为 **3/2 断路器**（500kV+ 为主，220kV 部分站亦用），则**重合闸逻辑在断路器保护（开关保护）中实现、非本线路保护**，且须独立分析**断路器失灵/死区/三相不一致/充电**动作。若事故目录含 `断路器保护`/`开关保护` 录波文件，本线路段落须注明"开关保护分析见其独立段落/简报 §6.7"。3/2 接线**无常规母差**，母差章节整篇 N/A。
5. 将电压等级、套别、接地方式、接线方式写入段落开头"**前置条件**"字段，供主 Agent 切换章节。

## 输入数据

### HDR 文件内容
{hdr_content if hdr_content else '[HDR文件缺失]'}

### RMS CSV 文件内容
{rms_content if rms_content else '[RMS文件缺失]'}

### Events CSV 文件内容
{events_content if events_content else '[Events文件缺失]'}

## 输出格式

**重要：直接输出 Markdown 内容，不要用 ```markdown``` 代码围栏包裹。**

严格按照以下 Markdown 格式输出。有数据的字段填写实际值，无数据的标注`[无数据]`，有推测规则的标注`[推测为XXX]`。

**精简原则**：
- 当整个区块无数据时（如 HDR 文件缺失），只写一行 `[HDR文件缺失]`，**不要**逐字段列出 [无数据]
- Events 表只列出"动作"事件，省略"返回"事件（减少冗余）
- RMS 信息只列出有突变值的通道
- **数字状态（DigitalStatus）只列出值为1的条目**，值为0的条目全部省略（包括"备用: 0"、未动作的保护功能等），不要逐条列出
- 录波器段落中的开关动作信息：同一设备同一事件（如断路器B相位置/总位置）只保留首次出现的一对（分位+合位），不要重复列出

输出格式模板（注意：模板中的 `{{xxx}}` 表示需要填入实际值的位置，无数据时表格内填 `-`，非表格位置写明缺失原因，**严禁输出 `{{xxx}}` 占位符**）：

## {station}_{set_number}

**装置型号**: {{型号}}，{{程序版本}}，{{智能站/常规站}}
**制造厂家**: {{厂家名称}}
**故障时间**: {{年}}-{{月}}-{{日}} {{时}}:{{分}}:{{秒}}.{{毫秒}}

### HDR信息
（如果 HDR 有内容：正常列出设备信息、TripInfo、DigitalEvent、FaultInfo、定值信息）
（如果 HDR 缺失或内容为空：只写 `[HDR文件缺失]`，不要展开子字段）

### Events信息
| 绝对时间 | 通道名称 | 内容 |
|----------|----------|------|
| HH:MM:SS.mmm | {{通道名}} | 动作 |

### RMS信息

**重要：rms.csv 中的"RMS最大值"和"RMS最小值"都是二次侧测量值（单位为A或V），不是一次值！**
- **RMS最大值** = 该通道在录波期间的二次侧RMS最大值（单位A或V）
- **RMS最小值** = 该通道在录波期间的二次侧RMS最小值（单位A或V），不是"二次值"的含义
- **一次值换算**：有CT/PT变比时，一次值 = 二次值 × CT一次额定值 / CT二次额定值 / 1000（电流kA）；电压类似
- **无CT/PT变比时**：只显示二次值，标注 [无CT变比] 或 [无PT变比]

**电流RMS最大值（二次值）**: IA: {{RMS最大值}}A, IB: {{RMS最大值}}A, IC: {{RMS最大值}}A, 3I0: {{RMS最大值}}A
（有CT变比时补充一次值: IA: {{一次值}}kA（二次值{{RMS最大值}}A，CT变比{{CT一次}}/{{CT二次}}））
（无CT变比时: IA: {{RMS最大值}}A [无CT变比]）
**电压RMS最小值（二次值）**: UA: {{RMS最小值}}V, UB: {{RMS最小值}}V, UC: {{RMS最小值}}V
（有PT变比时补充一次值: UA: {{一次值}}kV（二次值{{RMS最小值}}V，PT变比{{PT一次}}kV/{{PT二次}}V））
（无PT变比时: UA: {{RMS最小值}}V [无PT变比]）

### 差流分析

**差流数据（直接从文件提取，无需计算）**

| 通道 | A相差流 | B相差流 | C相差流 |
|------|---------|---------|---------|
| 通道一 | {{值}} A | {{值}} A | {{值}} A |
| 通道二 | {{值}} A | {{值}} A | {{值}} A |

- **故障相电流**: {{值}} A（来自 FaultInfo）
- **最大差动电流**: {{值}} A（来自 FaultInfo）
- **3I0**: {{值}} A（来自 FaultInfo）

（数据来自 HDR 的 TripInfo/FaultInfo，获取不到时写"未获取到数据"）

**结果汇总**

- **故障相**: {{相别}}（差流最大相）
- **最大差流**: {{一次值}} kA（通道一/通道二{{是否一致}}）
- **故障电流**: {{一次值}} kA
- **差流与故障电流比值**: {{百分比}}%
- **通道一致性**: 通道一与通道二差流{{是否一致}}，偏差{{百分比}}%（{{正常/异常}}）
（注意：故障相电流与差流是不同概念，不得混用）

**电流电压突变**（突变值为二次侧值，单位A或V）:
**第一次突变**（故障发生时）:
- {{通道名}}正突变: +{{值}}A @ HH:MM:SS.mmm
- {{通道名}}负突变: -{{值}}A @ HH:MM:SS.mmm
- ...（列出所有突变幅度超过阈值的通道）
- 如果某个通道没有检测到突变（突变值为空），说明该通道在故障期间变化平稳，不需要列出

**第二次突变**（重合后，如有）:
- {{通道名}}正突变: +{{值}}A @ HH:MM:SS.mmm
- ...（无第二次突变时不输出此分组）

### 定值与变比信息（必填区块）

**CT变比**:
- 一次额定电流: {{值}}A
- 二次额定电流: {{值}}A
- 变比倍数: {{N}}
- 数据来源: HDR SettingValue / [无数据]

**差动动作电流定值**:
- 差动动作电流定值: {{值}}A(二次)
- 折算一次值: {{N=变比}} × {{值}}A
- 本套录波差流二次最大值: {{值}}A(来源:{{RMS通道/HDR FaultInfo}})
- 差流一次值: {{二次值}} × {{变比}} / 1000 = {{X.XX}}kA
- 差流/定值倍数: {{倍数}} 倍

### 距离I段不动作原因（按需，当4套中均无距离I段动作信号时输出）

| 判别项 | 值 | 说明 |
|--------|----|------|
| 控制字(距离保护Ⅰ段有效) | {{0/1}} | 退出/已投入 |
| Ⅰ段定值(阻抗) | {{X.XX}}Ω | - |
| Ⅰ段时间定值 | {{X.XX}}s | - |
| Ⅰ段保护范围(估算) | {{X.XX}}km | Z1={{X.XX}}Ω/km |
| 本套测距(HDR) | {{X.XX}}km | - |
| 故障点 vs Ⅰ段范围 | {{在范围外/在范围内}} | 决定"应动未动"或"正确不动作" |
| 结论 | {{正确不动作 / 待核实: 应动未动}} | 与故障判定结论保持一致 |

### 故障发展过程数据
> 基于原始采样 CSV（如有）。**必做**：还原故障起始→发展→全盛→切除演化，取**本端（双端含对端）**三相电压/电流。无原始 CSV 时标注 `[无原始波形，故障发展过程无法量化]`。

**波形基础**: 采样率 {{fs}}Hz；{{有/无}}预故障段
**关键事件时刻（绝对）**:
- 故障电流启动: {{HH:MM:SS.mmm}}（{{相}}相 |I| 首超阈值）
- {{相}}相电压塌缩: {{HH:MM:SS.mmm}}
- 发展转折: {{HH:MM:SS.mmm}}（{{描述}}）
- 切除(电流归零): {{HH:MM:SS.mmm}}（重合前；如有第二次故障另列）

**各阶段三相 V/I RMS（二次值为主）**:
| 阶段 | UA/V | UB/V | UC/V | IA/A | IB/A | IC/A |
|------|------|------|------|------|------|------|
| 起始 | | | | | | |
| 发展 | | | | | | |
| 全盛 | | | | | | |
| 切除 | | | | | | |

**相间相关系数（全盛期）**: corr(A,B)={{值}}, corr(B,C)={{值}}, corr(A,C)={{值}}
**发展性判读**: {{是否发展性；起始类型→发展类型；电弧性/稳定性}}

> 数据来源: {{hdr文件名}} + {{rms文件名}} + {{events文件名}} + {{csv文件名}}

## 字段提取规则

### 数据来源优先级（来自 comtrade-web 最新规则）
- **优先从 HDR FaultInfo/故障信息中提取**
- 如果 HDR 无该信息，则从模拟通道 RMS 统计和突变数据中取值（标注"[推断]"）

### 故障类型判断
- 如果重合闸动作后线路持续运行（无后续分闸），则为"瞬时故障"
- 否则为"永久故障"（包括重合闸未动作、重合不成功等）

### 故障相别判断
- 从 HDR FaultInfo 中取
- 无则根据电压最小有效值通道和电流最大有效值通道推断

### 故障测距
- HDR 有则取（注意：0.00也是有效数据，必须如实填写"0.00km"）
- 无则标注"[无数据]"

### 时间格式规范
- **日期**：只写一次（在段落开头故障时间字段中）
- **绝对时间**：格式 `HH:MM:SS.mmm`
- **严禁输出相对时间**（如 `0ms`、`15ms`），所有时间必须为绝对时间 `HH:MM:SS.mmm`
- HDR 中时间是偏移量（如 `0ms`、`11ms`），需转换为绝对时间：绝对时间 = 故障起始时间 + ms偏移量

### 关键提取规则
1. 厂站名和套别：从目录结构或文件名中提取
2. 装置型号：从 HDR `<DeviceInfo>` 提取，根据型号后缀判断智能站/常规站（含 FA/DA/DG → 智能站，其他 → 常规站）
3. 故障时间：从 HDR `<FaultStartTime>` 提取
4. TripInfo：从 HDR `<TripInfo>` 提取，偏移量转绝对时间

   | HDR字段 | 说明 |
   |---------|------|
   | time | 偏移量（ms），需转换为绝对时间 |
   | name | 动作名称 |
   | phase | 相别（如ABC、A相、无） |
   | value | 1=动作，0=返回 |

   **转换方法**：绝对时间 = 故障起始时间 + time偏移
   例：故障起始 16:33:20.208，HDR中 time=107ms → 绝对时间 = 16:33:20.315

5. DigitalEvent：从 HDR `<DigitalEvent>` 提取

   | HDR字段 | 说明 |
   |---------|------|
   | time | 偏移量（ms），需转换为绝对时间 |
   | name | 事件名称 |
   | value | 变化描述（如 0→1、1→0） |

6. 定值信息：从 HDR `<SettingValue>` 提取关键定值：

   | 定值名称 | 说明 | 单位 |
   |----------|------|------|
   | 差动动作电流定值 | 差动保护动作电流定值 | A |
   | 线路总长度 | 线路全长 | km |
   | 接地距离Ⅰ段定值 | 接地距离I段阻抗定值 | Ω |
   | 接地距离Ⅱ段定值 | 接地距离II段阻抗定值 | Ω |
   | 接地距离Ⅱ段时间 | 接地距离II段时间 | s |
   | 相间距离Ⅰ段定值 | 相间距离I段阻抗定值 | Ω |
   | 零序过流Ⅲ段定值 | 零序过流III段电流定值 | A |
   | 停用重合闸 | 停用重合闸投退（1=停用） | - |
   | 单相重合闸时间 | 单相重合闸时间定值 | s |
   | 三相重合闸时间 | 三相重合闸时间定值 | s |
   | CT一次额定值 | CT一次侧额定电流 | A |
   | CT二次额定值 | CT二次侧额定电流（通常1A或5A） | A |
   | PT一次额定值 | PT一次侧额定电压 | kV |
   | 距离保护Ⅰ段有效 | 距离I段投退控制字（0=退出/1=投入） | - |
   | 接地距离Ⅰ段时间 | 接地距离I段时间定值 | s |
   | 零序电流补偿系数 | 零序电流补偿系数 | - |

   - 只提取与保护动作相关的定值和时限，不要提取非保护动作定值（如通道参数、装置参数等）

7. Events信息：从 events.csv 提取，保持原始通道名，只陈述事实
8. RMS信息：从 rms.csv 提取最大值/最小值/突变（按第一次/第二次分组，列出所有超过阈值的通道）。**rms.csv 中的值都是二次侧值**，有CT/PT变比时换算为一次值，无变比时只显示二次值
9. CT变比和PT变比：从 HDR `<SettingValue>` 提取（**不从 CFG 提取**，CFG 通常为 1:1 不准确）：

   | 字段名 | 说明 | 单位 |
   |--------|------|------|
   | CT一次额定值 | CT 一次侧额定电流 | A |
   | CT二次额定值 | CT 二次侧额定电流（通常 1A 或 5A） | A |
   | PT一次额定值 | PT 一次侧额定电压 | kV |

   **CT变比** = CT一次额定值 / CT二次额定值（如 3000/1）
   HDR 中无 CT/PT 变比字段时，标注 `[无数据]`。

10. FaultInfo：从 HDR `<FaultInfo>` 提取以下字段，全部换算为一次值：

    | 字段名 | 说明 | 显示格式 |
    |--------|------|----------|
    | 故障相电流 | 故障相电流二次值 | {{一次值}}kA（二次值{{值}}A，CT变比{{值}}/{{值}}） |
    | 最大差动电流 | 差动电流最大值 | 同上 |
    | 故障相电压 | 故障相电压二次值 | {{一次值}}kV（二次值{{值}}V，PT变比{{值}}kV/{{值}}V） |
    | 最大零序电流 | 零序电流最大值 | 同电流格式 |

    - **故障相电流**：用于故障基本情况表中的故障电流
    - **最大差动电流(Idiff)**：用于差流分析，必须明确标注为差流值
    - 注意区分：故障相电流 ≠ 差流，不得混用

11. 一次值换算：电流一次值(kA) = 二次值(A) × CT一次额定值 / CT二次额定值 / 1000；电压一次值(kV) = 二次值(V) × PT一次额定值(kV) / PT二次额定值(V)。无变比时只显示二次值并标注 [无CT变比] 或 [无PT变比]
12. 差流计算过程：输出时需展示换算过程，格式为 `{{二次值}}A × {{CT一次额定值}}/{{CT二次额定值}} = {{一次值}}kA`
    **计算示例（必须严格遵循）**：若二次值=6.68A，CT一次=3000A，CT二次=1A，则：6.68 × 3000 / 1 / 1000 = 20.04 kA。**结果必须是实际计算后的数值，不得将二次值原样作为一次值输出。**

13. **CFG 第1行读取规则**（装置型号/版本的第二来源）：
    - **国电南瑞 NSR（UTF-8+BOM 编码）**：CFG 第1行形如 `国网六统一版本,NSR-303A-DA-G-R,1999`：
      - field1 = 版本/平台标识（如 `国网六统一版本`，**这就是 NSR 的"版本号"**）
      - field2 = 完整装置型号（如 `NSR-303A-DA-G-R`，含 `-DA`→智能站）
      - field3 = CFG 版本年（如 `1999`）
      - ⚠️ NSR 的 HDR **无 `<DeviceInfo>` 块、也无"版本号"字段**——型号与版本**只在 CFG 第1行**，漏读 CFG 就会误判"NSR 基础信息缺失"
    - **南瑞继保 PCS/PRS（GB18030 编码）**：CFG 第1行形如 `南瑞继保,超高压输电线路成套保护装置,1999`：
      - field1 = 制造厂家（`南瑞继保`）、field2 = 装置描述（非数字型号）、field3 = 版本年
      - **精确数字型号来自 HDR `<DeviceInfo>` 或文件夹名**，CFG 第1行只给厂家+描述
    - **PT/CT 变比**：NSR CFG 通道比例常载真实一次/二次值（如 `220000/100`→PT变比2200）；PCS CFG 通道比例多为归一化 `1/1`，PT/CT 须改从 HDR `CT一次/二次额定值` 取

14. **距离I段不动作检查项**（必填，4套均无距离I段动作时强化输出）：

    **触发场景**: 四套装置Events信息中均无"接地距离Ⅰ段动作"信号。

    **14.1 距离保护Ⅰ段有效控制字**
    - 来源：HDR `<DigitalStatus>` 查找 `距离保护Ⅰ段有效` 或 `距离Ⅰ段投入` 字段
    - 值=0 → I段被退出（人为配置）
    - 值=1 → I段已投入，需进一步判别
    - 无字段 → 标注 `[无数据]`，默认按"退出"处理并写明依据

    **14.2 距离I段保护范围核算**
    保护范围(km) ≈ 接地距离Ⅰ段定值(Ω) ÷ Z1(Ω/km)
    - 来源1：HDR `<SettingValue>` 的 `接地距离Ⅰ段定值` (阻抗定值，单位Ω)
    - 来源2：HDR `<SettingValue>` 或扩展定值单的 `线路正序阻抗 Z1` (Ω/km) 或 `线路总长度+总阻抗` 反推

    **14.3 不动作原因判别规则**

    | 故障点 vs I段范围 | 控制字=0(退出) | 控制字=1(投入) |
    |---------|----------------|-----------------|
    | 故障点 > I段范围 | 正确不动作 | 正确不动作(物理上不会动作) |
    | 故障点 ≤ I段范围 | 正确不动作(主动退出) | **应动未动，待核实** |

    **14.4 注意事项**
    - 距离I段未动 ≠ 缺陷，必须按本节3步骤判别后才可下结论
    - 控制字=0 时，不动作 100% 正确；不可将此误判为"未投保护"
    - 控制字=1 + 故障点 > I段范围 时，仍属正确（物理不可达）
    - 控制字=1 + 故障点 ≤ I段范围 时，**必须**标 `[待核实: 距离I段应动未动]`，触发隐患规则

## 编码处理
HDR 可能为 GB18030 或 UTF-8 编码。若读取乱码，尝试另一种编码重新解析。

## 错误处理
- 文件缺失：标注`[本套文件缺失]`
- 字段无数据：表格内用 `-` 表示，非表格位置写"未获取到数据"
- 有推测规则且可推测：标注`[推测为XXX]`
- **严禁使用模板占位符**：输出中不得出现 `{{值}}`、`{{结果}}`、`{{厂家名称}}`、`{{程序版本}}` 等未替换的占位符

## 数据来源标注
在段落末尾标注数据来源文件。
"""
